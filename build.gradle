plugins {
    id 'java'
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {
    fileTree(dir: 'libs', include: '*.jar').each { file ->
        testImplementation name: file.name.replace('.jar', '')
    }
}

test {
    useJUnitPlatform()
}
